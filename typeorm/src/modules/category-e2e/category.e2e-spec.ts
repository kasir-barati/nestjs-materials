import { DefaultApi } from '../../api-client';

describe('Category (e2e)', () => {
    let apiClient: DefaultApi = new DefaultApi();

    beforeEach(async () => {});

    it('/categories (GET)', () => {
        const response = apiClient.; // do not have categories endpoints at all
    });
});
